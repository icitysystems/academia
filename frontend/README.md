# Academia Frontend (scaffold)

This is a minimal React + TypeScript + MUI + Apollo scaffold.

Quick start (PowerShell):

```powershell
cd frontend
npm install
npm start
```

Set `REACT_APP_GRAPHQL_URL` in `.env` to point to the backend GraphQL endpoint.
